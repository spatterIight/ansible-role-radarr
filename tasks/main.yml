# SPDX-FileCopyrightText: 2023 Slavi Pantaleev
# SPDX-FileCopyrightText: 2025 spatterIight
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
- name: Perform Radarr installation tasks
  when: radarr_enabled | bool
  tags:
    - setup-all
    - setup-radarr
    - install-all
    - install-radarr
  block:
    - name: Validate Radarr configuration
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/validate_config.yml"
    - name: Install Radarr
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/install.yml"

- name: Perform Radarr uninstallation tasks
  when: not radarr_enabled | bool
  tags:
    - setup-all
    - setup-radarr
  block:
    - name: Uninstall Radarr
      ansible.builtin.include_tasks: "{{ role_path }}/tasks/uninstall.yml"

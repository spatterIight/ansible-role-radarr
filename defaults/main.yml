# SPDX-FileCopyrightText: 2023, 2024 Nikita Chernyi
# SPDX-FileCopyrightText: 2023-2025 Slavi Pantaleev
# SPDX-FileCopyrightText: 2024 Sergio Durigan Junior
# SPDX-FileCopyrightText: 2025 MASH project contributors
# SPDX-FileCopyrightText: 2025 spatterIight
# SPDX-FileCopyrightText: 2025, 2026 Suguru Hirahara
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
# Project source code URL: https://github.com/Radarr/Radarr

radarr_enabled: true

radarr_identifier: radarr
radarr_base_path: "/{{ radarr_identifier }}"
radarr_data_path: "{{ radarr_base_path }}/data"

# renovate: datasource=docker depName=ghcr.io/linuxserver/radarr versioning=semver
radarr_version: 6.4.4
radarr_arch: amd64

radarr_uid: ""
radarr_gid: ""

# The hostname at which radarr is served.
radarr_hostname: ""

# The path at which radarr is served.
# This value must either be `/` or not end with a slash (e.g. `/radarr`).
radarr_path_prefix: /

radarr_container_image: "{{ radarr_container_image_registry_prefix }}linuxserver/radarr:{{ radarr_container_image_tag }}"
radarr_container_image_tag: "{{ radarr_version }}"
radarr_container_image_registry_prefix: "{{ radarr_container_image_registry_prefix_upstream }}"
radarr_container_image_registry_prefix_upstream: "{{ radarr_container_image_registry_prefix_upstream_default }}"
radarr_container_image_registry_prefix_upstream_default: ghcr.io/

# The base container network. It will be auto-created by this role if it doesn't exist already.
radarr_container_network: "{{ radarr_identifier }}"

# Control if the container network is deleted on uninstalling.
radarr_container_network_deletion_enabled: true

# Specify how the container publishes its HTTP port (as defined by `radarr_container_http_port`).
#
# Takes an "<ip>:<port>" or "<port>" value (e.g. "127.0.0.1:2586"), or empty string to not expose.
radarr_container_http_host_bind_port: ""

# A port number in the container
radarr_container_http_port: 7878

# A list of additional container networks that the container would be connected to.
# The role does not create these networks, so make sure they already exist.
# Use this to expose this container to another reverse proxy, which runs in a different container network.
radarr_container_additional_networks: "{{ (radarr_container_additional_networks_default + radarr_container_additional_networks_auto + radarr_container_additional_networks_custom) | unique }}"
radarr_container_additional_networks_default: []
radarr_container_additional_networks_auto: []
radarr_container_additional_networks_custom: []

# A list of additional "volumes" to mount in the container.
# This list gets populated dynamically at runtime. You can provide a different default value,
# if you wish to mount your own files into the container.
# Contains definition objects like this: `{"type": "bind", "src": "/outside", "dst": "/inside", "options": "readonly"}.
# See the `--mount` documentation for the `docker run` command.
radarr_container_additional_volumes: "{{ radarr_container_additional_volumes_auto + radarr_container_additional_volumes_custom }}"
radarr_container_additional_volumes_auto: []
radarr_container_additional_volumes_custom: []

# radarr_container_labels_traefik_enabled controls whether labels to assist a Traefik reverse-proxy will be attached to the container.
# See `../templates/labels.j2` for details.
#
# To inject your own other container labels, see `radarr_container_labels_additional_labels_custom`.
radarr_container_labels_traefik_enabled: true
radarr_container_labels_traefik_docker_network: "{{ radarr_container_network }}"
radarr_container_labels_traefik_hostname: "{{ radarr_hostname }}"
# The path prefix must either be `/` or not end with a slash (e.g. `/radarr`).
radarr_container_labels_traefik_path_prefix: "{{ radarr_path_prefix }}"
radarr_container_labels_traefik_rule: "Host(`{{ radarr_container_labels_traefik_hostname }}`){% if radarr_container_labels_traefik_path_prefix != '/' %} && PathPrefix(`{{ radarr_container_labels_traefik_path_prefix }}`){% endif %}"
radarr_container_labels_traefik_priority: 0
radarr_container_labels_traefik_entrypoints: web-secure
radarr_container_labels_traefik_tls: "{{ radarr_container_labels_traefik_entrypoints != 'web' }}"
radarr_container_labels_traefik_tls_certResolver: default # noqa var-naming

# Controls which additional headers to attach to all HTTP requests.
# To add your own custom request headers, use `radarr_container_labels_traefik_additional_request_headers_custom`
radarr_container_labels_traefik_additional_request_headers: "{{ radarr_container_labels_traefik_additional_request_headers_auto | combine(radarr_container_labels_traefik_additional_request_headers_custom) }}"
radarr_container_labels_traefik_additional_request_headers_auto: {}
radarr_container_labels_traefik_additional_request_headers_custom: {}

# Controls which additional headers to attach to all HTTP responses.
# To add your own custom response headers, use `radarr_container_labels_traefik_additional_response_headers_custom`
radarr_container_labels_traefik_additional_response_headers: "{{ radarr_container_labels_traefik_additional_response_headers_auto | combine(radarr_container_labels_traefik_additional_response_headers_custom) }}"
radarr_container_labels_traefik_additional_response_headers_auto: |
  {{
    {}
    | combine ({'X-XSS-Protection': radarr_http_header_xss_protection} if radarr_http_header_xss_protection else {})
    | combine ({'X-Content-Type-Options': radarr_http_header_content_type_options} if radarr_http_header_content_type_options else {})
    | combine ({'Content-Security-Policy': radarr_http_header_content_security_policy} if radarr_http_header_content_security_policy else {})
    | combine ({'Permissions-Policy': radarr_http_header_permissions_policy} if radarr_http_header_permissions_policy else {})
    | combine ({'Strict-Transport-Security': radarr_http_header_strict_transport_security} if radarr_http_header_strict_transport_security and radarr_container_labels_traefik_tls else {})
  }}
radarr_container_labels_traefik_additional_response_headers_custom: {}

# radarr_container_labels_additional_labels contains a multiline string with additional labels to add to the container label file.
# See `../templates/labels.j2` for details.
# To add your own custom labels, use `radarr_container_labels_additional_labels_custom`
#
# Example:
# radarr_container_labels_additional_labels_custom:
#   - my.label=1
#   - another.label="here"
radarr_container_labels_additional_labels: "{{ radarr_container_labels_additional_labels_auto + radarr_container_labels_additional_labels_custom }}"
radarr_container_labels_additional_labels_auto: []
radarr_container_labels_additional_labels_custom: []

# A list of extra arguments to pass to the container (`docker create` command)
radarr_container_extra_arguments: "{{ radarr_container_extra_arguments_auto + radarr_container_extra_arguments_custom }}"
radarr_container_extra_arguments_auto: []
radarr_container_extra_arguments_custom: []

# A list of extra arguments to pass to the process inside the container
# Example: ['--SOME_ARGUMENT']
radarr_process_extra_arguments: "{{ radarr_process_extra_arguments_auto + radarr_process_extra_arguments_custom }}"
radarr_process_extra_arguments_auto: []
radarr_process_extra_arguments_custom: []

# Specifies the value of the `X-XSS-Protection` header
# Stops pages from loading when they detect reflected cross-site scripting (XSS) attacks.
#
# Learn more about it is here:
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-XSS-Protection
# - https://portswigger.net/web-security/cross-site-scripting/reflected
radarr_http_header_xss_protection: "1; mode=block"

# Specifies the value of the `X-Content-Type-Options` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Content-Type-Options
radarr_http_header_content_type_options: nosniff

# Specifies the value of the `Content-Security-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy
radarr_http_header_content_security_policy: frame-ancestors 'self'

# Specifies the value of the `Permissions-Policy` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Permissions-Policy
radarr_http_header_permissions_policy: "{{ 'interest-cohort=()' if radarr_floc_optout_enabled else '' }}"

# Specifies the value of the `Strict-Transport-Security` header.
# See: https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
radarr_http_header_strict_transport_security: "max-age=31536000; includeSubDomains{{ '; preload' if radarr_hsts_preload_enabled else '' }}"

# Controls whether to send a "Permissions-Policy interest-cohort=();" header along with all responses
#
# Learn more about what it is here:
# - https://www.eff.org/deeplinks/2021/03/googles-floc-terrible-idea
# - https://paramdeo.com/blog/opting-your-website-out-of-googles-floc-network
# - https://amifloced.org/
#
# Of course, a better solution is to just stop using browsers (like Chrome), which participate in such tracking practices.
# See: `radarr_http_header_permissions_policy`
radarr_floc_optout_enabled: true

# Controls if HSTS preloading is enabled
#
# In its strongest and recommended form, the [HSTS policy](https://www.chromium.org/hsts) includes all subdomains, and
# indicates a willingness to be "preloaded" into browsers:
# `Strict-Transport-Security: max-age=31536000; includeSubDomains; preload`
# For more information visit:
# - https://en.wikipedia.org/wiki/HTTP_Strict_Transport_Security
# - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Strict-Transport-Security
# - https://hstspreload.org/#opt-in
# See: `radarr_http_header_strict_transport_security`
radarr_hsts_preload_enabled: false

# List of systemd services that the Radarr systemd service depends on
radarr_systemd_required_services_list: "{{ (radarr_systemd_required_services_list_default + radarr_systemd_required_services_list_auto + radarr_systemd_required_services_list_custom) | unique }}"
radarr_systemd_required_services_list_default: "{{ [devture_systemd_docker_base_docker_service_name] if devture_systemd_docker_base_docker_service_name else [] }}"
radarr_systemd_required_services_list_auto: []
radarr_systemd_required_services_list_custom: []

# List of systemd services that the Radarr systemd service wants
radarr_systemd_wanted_services_list: "{{ (radarr_systemd_wanted_services_list_default + radarr_systemd_wanted_services_list_auto + radarr_systemd_wanted_services_list_custom) | unique }}"
radarr_systemd_wanted_services_list_default: []
radarr_systemd_wanted_services_list_auto: []
radarr_systemd_wanted_services_list_custom: []

# Controls how long to wait for the container to stop gracefully before killing it.
radarr_container_stop_grace_time_seconds: "{{ devture_systemd_docker_base_container_stop_grace_time_seconds }}"

# Controls how long the pre-start cleanup should wait for stale container-name release
# before failing startup explicitly.
radarr_container_cleanup_timeout_seconds: "{{ devture_systemd_docker_base_container_cleanup_timeout_seconds }}"

# Controls the delay between pre-start cleanup retries.
radarr_container_cleanup_retry_delay_seconds: "{{ devture_systemd_docker_base_container_cleanup_retry_delay_seconds }}"

# Shell command used in the systemd pre-start phase to deterministically remove a stale
# container and wait until Docker releases its name.
radarr_container_cleanup_command: |
  i=0;
  while [ "$i" -lt {{ radarr_container_cleanup_timeout_seconds }} ]; do
  {{ devture_systemd_docker_base_host_command_docker }} stop -t {{ radarr_container_stop_grace_time_seconds }} {{ radarr_identifier }} >/dev/null 2>&1 || true;
  {{ devture_systemd_docker_base_host_command_docker }} rm -f {{ radarr_identifier }} >/dev/null 2>&1 || true;
  if {{ devture_systemd_docker_base_host_command_docker }} container inspect {{ radarr_identifier }} >/dev/null 2>&1; then
  i=$((i + 1));
  sleep {{ radarr_container_cleanup_retry_delay_seconds }};
  continue;
  fi;
  if {{ devture_systemd_docker_base_host_command_docker }} info >/dev/null 2>&1; then
  exit 0;
  fi;
  echo "Failed to query Docker daemon while cleaning up {{ radarr_identifier }}" >&2;
  exit 1;
  done;
  echo "Failed to remove stale container {{ radarr_identifier }} after {{ radarr_container_cleanup_timeout_seconds }} attempts" >&2;
  exit 1

# Specify the timezone
radarr_environment_variables_tz: "Etc/UTC"

# radarr_environment_variables_additional_variables contains a multiline string with additional environment variables to pass to the container.
#
# Example:
# radarr_environment_variables_additional_variables: |
#   VAR=1
#   ANOTHER=value
radarr_environment_variables_additional_variables: ""

# radarr_restart_necessary controls whether the Radarr systemd
# service will be restarted (when true) or merely started (when false) by the
# systemd service manager role (when conditional restart is enabled).
#
# This value is automatically computed during installation based on whether
# any configuration files, the systemd service file, or the container image changed.
# The default of `false` means "no restart needed" — appropriate when the role's
# installation tasks haven't run (e.g., due to --tags skipping them).
radarr_restart_necessary: false
